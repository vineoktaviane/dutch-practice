# Reference material

Not built, not imported, not maintained. Kept as the record of where this app
came from.

- `dutch-grammar.html` — the single-file prototype this project was ported from.
  `src/storage.ts` still documents behaviour inherited from it.
- `generator-stress-test.js` — the node script that `tests/generators.test.ts`
  was ported from.
